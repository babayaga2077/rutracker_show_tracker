python-telegram-bot[job-queue]==21.6
requests==2.32.3
beautifulsoup4==4.12.3
python-dotenv==1.0.1
# Impersonates a real Chrome TLS fingerprint. Without it Cloudflare challenges
# every request regardless of cookies.
curl_cffi==0.7.4
